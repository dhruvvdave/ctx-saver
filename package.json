{
  "name": "ctx-saver",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "backend": "npm run dev --workspace=packages/backend",
    "cli": "npm run build --workspace=packages/cli",
    "build": "npm run build --workspaces"
  }
}
